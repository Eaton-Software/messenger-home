machine:
    services:
        - docker
    node:
        version: stable
deployment:
    hub:
        branch: scripts
        commands:
            - docker info
            - docker build -t wijagels/messenger-home ~/messenger-home
            - docker login -e $DOCKER_EMAIL -u $DOCKER_USER -p $DOCKER_PASS
            - docker push wijagels/messenger-home
